<div>

<% if !@transactions.nil? %>
<% @transactions.each do |t| %>
  <%= t.name  %>
  <% end %>
  
<% end %></div>
<div>
<%= link_to 'new transaction', new_transaction_path, class:'btn btn-primary' %>
</div>